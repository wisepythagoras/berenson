import React from 'react';
import { View } from 'react-native';
import { Text } from 'react-native-paper';
import { SHAHash } from '../../recipes/hashing/sha';

/**
 * Renders the SHA hashing route.
 */
export const SHAHashRoute = () => {
    return (
        <View>
            <Text>
                Hello SHA
            </Text>
        </View>
    );
};
